import styles from "./singleOrderDateChange.module.scss";

function SingleOrderDateChange({ children }) {
  return <div className={styles.container}>{children}</div>;
}

export default SingleOrderDateChange;
